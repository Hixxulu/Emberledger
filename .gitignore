# Copyrighted books — never push these to a public repo
Shadowdark-Core/
Shadowdark-Zines/
Vermis/

# Local machine settings
.claude/settings.local.json

# Party data snapshots — private, restore from these locally
# (the .ps1 helper scripts in backups/ ARE tracked; only data stays out)
backups/*.json
